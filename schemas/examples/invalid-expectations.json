{
  "description": "Negative examples and the failure each one must produce. A negative example that fails for the wrong reason is as much a bug as one that passes, so CI matches the reported error rather than only the exit code.",
  "cases": [
    {
      "file": "finding.invalid.json",
      "schema": "finding.schema.json",
      "violates": "kind and source pairing",
      "expected_error_contains": "/source",
      "rationale": "kind is declared_egress_point, which forces source to be declared. This exercises the conditional block, not just field presence."
    },
    {
      "file": "finding.operation-case.invalid.json",
      "schema": "finding.schema.json",
      "violates": "operation spelling is fixed by the event contract",
      "expected_error_contains": "/operation",
      "rationale": "The reconciliation join compares this field against the operation a runtime hook recorded, and the hook writes lower case by contract. A finding spelled Chat.Completions.Create would validate as a string, join against nothing, and report a code point that did run as dormant. The pattern is what turns that silent mismatch into a rejection."
    },
    {
      "file": "report.no-coverage.invalid.json",
      "schema": "report.schema.json",
      "violates": "coverage statement is mandatory",
      "expected_error_contains": "coverage",
      "rationale": "A report without a coverage statement must be rejected at schema level. This is the honest coverage principle made unbreakable rather than merely documented."
    },
    {
      "file": "egress-event.invalid.json",
      "schema": "egress-event.schema.json",
      "violates": "payload shape may not carry content",
      "expected_error_contains": "raw_body",
      "rationale": "The hook records structure, never content. A raw_body field would turn an observation tool into a copy of the data it was deployed to protect, so the schema refuses the field outright rather than trusting every caller to leave it out."
    },
    {
      "file": "hook-status.invalid.json",
      "schema": "hook-status.schema.json",
      "violates": "the observation window is a duration, never a wall clock stamp",
      "expected_error_contains": "/observation_window_ms",
      "rationale": "A stamp in this field would put a clock value into the one number every dormant_egress_point finding rests on, and two runs over the same observations would then reconcile differently. The field is also what tells a reader whether the hook was watching for ten minutes or ten milliseconds, so a string that merely looks temporal must be rejected rather than parsed leniently by whichever reader gets there first."
    },
    {
      "file": "flow.invalid.json",
      "schema": "flow.schema.json",
      "violates": "a flow may not carry payload bytes",
      "expected_error_contains": "payload_bytes",
      "rationale": "The sensor observes destinations, not contents. A field for payload bytes would turn the component deployed to find leaks into one, so the schema refuses it rather than relying on every implementation to leave it out."
    },
    {
      "file": "flow.sni-without-handshake.invalid.json",
      "schema": "flow.schema.json",
      "violates": "a server name may only exist where the handshake showed one",
      "expected_error_contains": "sni_source",
      "rationale": "encrypted_client_hello measures a blind spot: the name was genuinely unavailable. A record that carries a name anyway contradicts the field measuring the loss, and the contradiction would be read as data rather than as a broken producer. This is the honest coverage principle enforced by the validator instead of by review."
    }
  ]
}
